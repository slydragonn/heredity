# Heredity App

## Iniciar Proyecto

```bash
# Instalar Dependencias
npm install

# Compilar contrato
npx hardhat compile

# Desplegar contrato
npx hardhat node

npx hardhat run scripts/deploy.ts --network localhost
```